<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orizon.shashank_ayush.barcontra">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.orizon.shashank_ayush.barcontra.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".child_login"
            android:label="@string/title_activity_child_login" />
        <activity android:name="com.orizon.shashank_ayush.barcontra.clhild_login" />
        <activity android:name="com.orizon.shashank_ayush.barcontra.parent_signin" />
        <activity android:name="com.orizon.shashank_ayush.barcontra.parent_signup" />

        <service
            android:name="com.orizon.shashank_ayush.barcontra.TrackerService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>